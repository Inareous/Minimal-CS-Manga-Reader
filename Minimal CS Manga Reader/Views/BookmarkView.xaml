<UserControl x:Class="Minimal_CS_Manga_Reader.BookmarkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="400">
    <UserControl.Resources>
            <Storyboard x:Key="OverlayFastSemiFadeIn" 
             AccelerationRatio="1" 
             BeginTime="0:0:0" 
             SpeedRatio="50" 
             Storyboard.TargetProperty="Opacity">
                <DoubleAnimation To="50" />
            </Storyboard>
            <Storyboard x:Key="OverlayFastSemiFadeOut" 
             AccelerationRatio="0" 
             BeginTime="0:0:0" 
             SpeedRatio="5" 
             Storyboard.TargetProperty="Opacity">
                <DoubleAnimation To="60" />
            </Storyboard>
    </UserControl.Resources>
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="500" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Background="{DynamicResource MaterialDesignPaper}" CornerRadius="10">
            <StackPanel Margin="30 30 30 30">
                <ListBox x:Name="Bookmarks" Height="500">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0">
                                        <StackPanel>
                                            <TextBlock FontSize="14" Grid.Column="0" Padding="1" Text="{Binding ChapterPathTrimmed}"/>
                                        <TextBlock FontSize="13" Grid.Column="0" Padding="1" Text="{Binding ActiveChapterEntry.Name}"/>
                                        </StackPanel>
                                    </Border>
                                    <Button Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center"
                                        Margin="1 0 5 0" Style="{DynamicResource MaterialDesignFlatButton}"
                                        Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}, Path=ViewModel.DeleteBookmark}"
                                        CommandParameter="{Binding .}">
                                        <Canvas Margin="-25,-25,0,0" VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" Fill="MediumPurple" />
                                        </Canvas>
                                    </Button>
                                </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>

                            <Setter Property="Padding" Value="5"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Margin="10" x:Name="Open" Content="Open" />
                    <Button Grid.Column="1" Margin="10" x:Name="Cancel" Content="Cancel" />
                </Grid>
            </StackPanel>
        </Border>
        
    </Grid>
</UserControl>